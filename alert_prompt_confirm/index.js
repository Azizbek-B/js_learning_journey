// Alert is a function with what we can notify users 

// prompt  is a function that allows user to input a text
let result = prompt("write something");

alert(result);

// confirm fubction shows a window on the browser with yok and cancel button
let confirmation = confirm("press one of the buttons");

alert(confirmation)

// confirm returns true if user presses ok and false if cancel
